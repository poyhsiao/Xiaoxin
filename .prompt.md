我想要做一個類似於 toby - https://www.gettoby.com/ 的書籤管理平台，有以下功能特徵：
- 每個帳號都可以建立多個 organization ，這些 organization 可以根據喜好，訂閱/分享
- 每個 organization 可以有不同的 spaces
- 每個 space 也可以加入不同的 collections ，每個 collection 都可以加入書籤
- organization 可以根據設定，決定是否要公開、被邀請、私人等設定，公開或是可被邀請的 organization 可以動態更新，也可以授權是否與其他人共用管理權限，或是只有管理者(分享者)自己可以管理
- 應該使用 client/server 架構，client 端應該可以在 chrome, safari, firefox 等主流瀏覽器中，使用 extension 方式安裝管理，也有 ios/android 可以使用的 app，另外也有 web 網頁管理功能。
- server 端應該使用 docker 方式建立部署，使用 SaaS 架構，提供 RESTful API (openapi) 功能
